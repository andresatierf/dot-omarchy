;; vim: set ft=lisp
(defcfg
  process-unmapped-keys yes
)

(defsrc
  caps
)

(defvar
  tap-time 150
  hold-time 250
)

(defalias
  escctrl (tap-hold $tap-time $hold-time esc lctl)
)

(deflayer base
  @escctrl
)
